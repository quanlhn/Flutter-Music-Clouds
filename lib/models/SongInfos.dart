class SongInfo {
  String songName;
  String imageUrl;
  String songUrl;
  String artistName;
  int like;
  int listened;
  String type;

  SongInfo(this.songName, this.imageUrl, this.songUrl, this.artistName, this.like, this.listened, this.type);

  // void print() {
  //   print(songName);
  // }
}
